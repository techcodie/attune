# Keep dependency resolution strict and deterministic across the workspace.
auto-install-peers=true
strict-peer-dependencies=false
# Allow workspace packages to be linked by their exact version.
link-workspace-packages=true
save-workspace-protocol=rolling
